class KeyCommandManager(){
	constructor(){
		this.keyStream = '';
		this.commands = {};
		this.registerKeyCommand = this.registerKeyCommand.bind(this);
	}

	registerKeyCommand(command, callback){
		
	}

}